URL: URL: https://github.com/rachelm87/DI-Bootcamp/blob/main/Week3/Day2/ExerciseXP/ExercisesXP.py
suggestions for improvement:
- In Exercise 1, the `Chartreux` and `Siamese` classes don't add any unique attributes or methods,  consider adding breed-specific characteristics to make them more distinct.  For instance,  a `Chartreux` might have a specific vocalization method, while `Siamese` could have a distinct fur pattern attribute.
- In Exercise 1, the `Bengal` class has additional attributes (`weight`, `color`).  The provided example doesn't include these, making it inconsistent.  Either remove them from `Bengal` or add corresponding attributes to `Cat` and other subclasses for consistency.
- In Exercise 1, add some more cats to `all_cats` list to make the output more comprehensive.
- In Exercise 3,  the `play()` method's docstring should clearly explain how `*args` is used (to accept multiple dog objects).
- In Exercise 3, add error handling (e.g., a `try...except` block) to the `do_a_trick()` method in case `tricks` list is empty. Consider a more robust way of selecting a random trick (ensure there's at least one trick available before attempting to select a random one).
- In Exercise 4, improve the `family_presentation()` method by printing the last name of each family member, not just the family name, for a clearer presentation.
Correctness: 95%
-----
URL: URL: https://github.com/rachelm87/DI-Bootcamp/blob/main/Week3/Day2/ExerciseXP/ExercisesXP3.py
suggestions for improvement:
- The `play` method could be improved to include the names of all the dogs playing together.  Currently, it only prints a generic message.
- Add error handling for the case where `__init__` from `Dog` class expects different parameters. Add docstrings to methods for better readability and understanding.
- The `ExercisesXP` import is vague.  Consider specifying the full path or using a more descriptive module name.
Correctness: 90%
-----